from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='just testing out versioning using dvc and git',
    author='adi',
    license='',
)
